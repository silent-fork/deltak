# Node / Next.js
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo
next-env.d.ts

# Environment — never commit credentials
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log

# Backtest pipeline — regenerate locally, too large / credential-dependent to commit
backtest/deep_data/
backtest/rolling_data/
backtest/*.pkl
