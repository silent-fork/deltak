# Base URL of the Delta-K FastAPI engine. Next.js rewrites /api/* to it, so the
# browser only ever talks to its own origin (no CORS, and SSE works cleanly).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
